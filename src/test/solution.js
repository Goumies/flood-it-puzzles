const {describe, it} = require('mocha');
const chai = require('chai');

const assert = chai.assert;
const should = chai.should();

describe ('Flood it Puzzles', () =>{
  describe('Classic', () => {
    it('should clear the board', () => {
      (0).should.equal(1)
    })
  })
})
